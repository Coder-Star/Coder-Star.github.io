---
title: 推送相关
category:
  - iOS
  - 相关
tags:
  - iOS
  - 推送
date: 2021-01-28 16:07:12
---

iOS 推送分为本地推送、在线推送、离线推送。

- 本地推送就是本地可以通过固定时间以及地点等条件触发推送操作。
- 在线推送就是 APP 在前台利用长链接等形式实现推送，如 QQ、微信在前台时收到的消息；
- 离线推送是指通过 APNs 服务器发送通知到 APP。

## 离线推送

推送流程基本如下：

1. APP 申请推送权限
2. APP 申请获得 deviceToken
3. APNs 服务器验证成功后，将 deviceToken 返回至 APP 端
4. APP 端将 deviceToken 存储至服务器，服务器带着证书以及 deviceToken 发送到 APNs 服务器
5. APNs 服务器下发推送消息到 APP 端

> APP 申请推送权限的时候会将设备的 device id 以及 APP 的 bundle id 以及证书等上报至 APNs 服务器，APNs 服务器会根据这两个信息生成 deviceToken。但是 device id 和 bundle id 不会确定唯一的 device token，对于同一部手机，卸载后重装 APP 的 device token 是会发生变化的，而且老的 token 不会无效，还可以正常推送。这应该是苹果的一个 Bug。

```swift

let center = UNUserNotificationCenter.current()
center.delegate = self
center.getNotificationSettings { [weak self] settings in
    switch settings.authorizationStatus {
    // 权限通过
    case .authorized:
    // 没有请求授权过，请求权限
    case .notDetermined:
       center.requestAuthorization(options: [.alert, .sound, .badge]) { accepted, error in
          if accepted {
            // 通过权限
          } else {
            // 权限未通过
           }
         }
    // 权限关闭
    case .denied:
    case .provisional:
    case .ephemeral:
    }
}

// 向APNs请求deviceToken
UIApplication.shared.registerForRemoteNotifications()
```

相关回调方法

```swift
// deviceToken注册成功
func application(_ application: UIApplication, didRegisterForRemoteNotificationsWithDeviceToken deviceToken: Data) {
   // 注意返回的deviceToken是一个16进制的Data型，需要自己进行转化
   let token = map { String(format: "%02x", deviceToken) }.joined()
}

// token注册失败，回调失败原因
func application(_ application: UIApplication, didFailToRegisterForRemoteNotificationsWithError error: Error) {

}

/// 静默推送时回调此方法，大概可以执行30s，APP处于前台、后台时都可回调此方法
func application(_ application: UIApplication, didReceiveRemoteNotification userInfo: [AnyHashable: Any], fetchCompletionHandler completionHandler: @escaping (UIBackgroundFetchResult) -> Void) {
    // 此闭包告诉系统是否执行完毕
    completionHandler(.noData)
}

/// APP处于前台时才会回调该方法，iOS10后新增的方法
func userNotificationCenter(_ center: UNUserNotificationCenter, willPresent notification: UNNotification, withCompletionHandler completionHandler: @escaping (UNNotificationPresentationOptions) -> Void) {
    guard let trigger = notification.request.trigger else {
        completionHandler([.alert, .sound, .badge])
            return
    }

    if trigger.isKind(of: UNPushNotificationTrigger.self) {
            /// 远程推送
    } else if trigger.isKind(of: UNLocationNotificationTrigger.self) {
            /// 本地推送
    }
    /// 回调的设置决定前台时推送来时的效果
    completionHandler([.alert, .sound, .badge])
}

/// 点击推送消息，iOS10后新增的方法
func userNotificationCenter(_ center: UNUserNotificationCenter, didReceive response: UNNotificationResponse, withCompletionHandler completionHandler: @escaping () -> Void) {
    guard let trigger = response.notification.request.trigger else {
            completionHandler()
            return
    }
    if trigger.isKind(of: UNPushNotificationTrigger.self) {
      // 远程推送
    } else if trigger.isKind(of: UNLocationNotificationTrigger.self) {
      /// 本地推送
    }
      completionHandler()
}

```

一般需要静默推送，需要开启 Background Modes 里面的 Remote notifications 选项。静默推送满足条件如下：

- 不能加 alert
- 加"content-available" : 1
- 不加 sound

iOS 新增了`NotificationServiceExtension`以及`NotificationServiceExtension`。

NotificationServiceExtension：当系统收到通知后会唤醒它，并允许它修改通知内容，之后再展示通知。如果想唤起 NotificationServiceExtension，payload 必须符合这样几个条件：

- 必须增加 mutable-content 字段并为 1，这表示允许客户端修改这个通知；
- 这个通知必须展示一个 alert，如果只是一个修改 badge 的通知的话，是不会唤起 service extension 的；
- 静默推送是不能唤起 service extension 的，所以 payload 中不能有”content-available” : 1 字段；

使用场景：

- 可以实现 push 到达率统计功能
- 可以实现不启动 APP，下载小文件功能
- 可以进行 push 信息脱敏处理功能
- 可以更改提示音功能

NotificationServiceExtension：自定义推送展开后的视图。

payload

```

```
