---
title: SQL语句优化
date: 2019-10-20 19:54:36
category:
  - 数据库
tags:
  - 数据库
---

- 比较运算符能用“=”就不用“<>”
  > "="可以增加索引的使用几率；
- **如果确定只有查询出来的结果只有一个，使用 “LIMIT 1”进行限制**
  > “LIMIT 1”可以避免全表扫描，找到对应结果就不会再继续扫描了;
- 为列选择合适的数据类型
  > 能用 SMALLINT 就不用 INT，减少磁盘和内存消耗；
- 为获得相同结果集的多次执行，请保持 SQL 语句前后一致
  > 前后一致表示语句完全一致，包括属性的先后顺序；
- 尽量避免使用“SELECT \*”
  > 如果不查询表中所有的列，尽量避免使用 SELECT \*，因为它会进行全表扫描，不能有效利用索引，增大了数据库服务器的负担，以及它与应用程序客户端之间的网络 IO 开销;
